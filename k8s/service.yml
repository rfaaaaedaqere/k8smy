apiVersion: v1
kind: Service
metadata:
  name: yoauyof
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yoauyof"
    port: 8080
  selector:
    app: yoauyof
